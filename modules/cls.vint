package cls {
    let init = func() {
        // println("Init from cls") //This will run when the package is imported
        cls.clear();
    }
    const shell = import("shell");
    let clear = func() {
        return println("\x1B[2J\x1B[H") // ANSI escape codes to clear terminal
        // return shell.run("clear");
    }
}